# ##### 星号（*）乘法运算
# 对数组执行对应位置相乘
# 对矩阵执行矩阵乘法运算

import numpy as np
A = np.array([[1, 2],
              [3, 4]])
B = np.array([[0, 1],
              [2, 3]])
print(A*B)

print((np.mat(A))*(np.mat(B)))

print(a ** b)
